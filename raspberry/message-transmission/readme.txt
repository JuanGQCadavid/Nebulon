NEEDED TO PROPERLY RUN THE PROGRAM

1. Python library installer 'pip'. Usually package python3-pip
2. Run the command
   $ sudo pip install mysql-connector-python
   $ sudo pip install mysql-connector

------------

PROBLEMS

1. Raspi shows: mysql module Not found, even after running
   sudo pip install mysql-connector-python mysql-connector
